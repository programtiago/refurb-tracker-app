spring:
  datasource:
    url: jdbc:h2:mem:erp-mock-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    generate-unique-name: false
  driverClassName: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
  show-sql: true

  h2:
    console:
      enabled: true
      path: /h2
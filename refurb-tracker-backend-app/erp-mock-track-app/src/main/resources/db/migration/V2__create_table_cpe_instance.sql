CREATE TABLE TB_CPE_INSTANCE (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
    status VARCHAR(50),
    cpe_id BIGINT NOT NULL,
    CONSTRAINT fk_cpe FOREIGN KEY (cpe_id) REFERENCES TB_CPE(id)
);
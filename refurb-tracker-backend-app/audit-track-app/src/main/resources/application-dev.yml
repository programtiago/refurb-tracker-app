spring:
  datasource:
    url: jdbc:h2:file:./backend/db/refurb-audit
    username: sa
    password:
  driverClassName: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
  show-sql: true

  h2:
    console:
      enabled: true
      path: /h2